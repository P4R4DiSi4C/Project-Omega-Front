import Styled from 'styled-components';

export default {
    Layout: Styled.div`
        display:flex;
        align-items:center;
        width:100%;
        justify-content:flex-start;

        border:solid;
        border-color:green;
    `
};